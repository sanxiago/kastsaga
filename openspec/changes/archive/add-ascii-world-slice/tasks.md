# Tasks — add-ascii-world-slice

- [x] Draft spec delta for new capability `world-rendering` (ASCII view): requirements for rendering observations to a grid of glyphs, legend, viewport rules, and emote/status display.
- [x] Draft spec delta for `world-state` baseline grid profile: tiles (terrain), objects, entities, boundaries, and deterministic layout for the ASCII slice.
- [x] Ensure emote/status display is defined (what data from world/observation is rendered, not how it’s generated).
- [x] Keep deltas engine-agnostic and compatible with future 2D rendering; note ASCII is a diagnostic/minimal slice.
- [x] Validate the change (`openspec validate add-ascii-world-slice --strict`) once authored.
- [x] Review against Constitution (P1, P2, P5, P6, P9, P10) and `DISCUSSION.md` open questions to ensure no conflicts.
