# Tasks — add-ascii-movement-demo

- [x] Factor renderer into a reusable module (no behavior changes) and keep CLI `render.js` working.
- [x] Add movement demo `examples/ascii-world/move.js` with arrow-key input, blocking non-traversable tiles/bounds, updating player position, and re-rendering with legend.
- [x] Update `examples/ascii-world/README.md` with movement instructions and controls.
- [ ] Verify demo manually (render, move, boundary blocking) and keep output deterministic per state.
- [x] Note that no spec deltas are included; this is an example implementation.
